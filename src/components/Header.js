import React from 'react'
import { Container } from 'react-bootstrap'

function Header(){
    return(
        <div style={{height: "50px"}} class="bg-primary">
            <div class="  text-center fs-2">POMODORO TIMER</div>
        </div>
    )
}

export default Header;